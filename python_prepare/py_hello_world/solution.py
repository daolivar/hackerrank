# This line prints the message "Hello, World!" directly to the console
print("Hello, World!")

# An alternative approach:
# We can first create a string variable and then pass it into the print function
message = "Hello, World!"  # Store the message in a variable
print(message)  # Print the contents of the variable
